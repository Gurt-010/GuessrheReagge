<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for streaming audio from YouTube Music endpoints -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Cast device discovery on the local Wi-Fi network -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />

    <application
        android:name=".GuessrheApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuessrheReagge"
        tools:targetApi="31">

        <!-- Google Cast SDK: receiver app ID and options provider -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.soundstation.guessrhereagge.cast.CastOptionsProvider" />

        <!--
            Adaptive launcher icon configuration:
            - @mipmap/ic_launcher       → res/mipmap-anydpi/ic_launcher.xml
            - @mipmap/ic_launcher_round → res/mipmap-anydpi/ic_launcher_round.xml
            Both reference layered background/foreground drawables with monochrome support.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.GuessrheReagge"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
